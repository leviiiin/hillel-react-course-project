import "./Form.css";
import Input from "../Input/Input";
import Button from "../Button/Button";

const Form = () => {
  return (
    <form className="login-form">
      <Input/>
      <Button />
    </form>
  );
};

export default Form;
